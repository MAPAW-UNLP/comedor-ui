/**
 * Objeto que contiene las variables de entorno usadas en la aplicación para un determinado entorno de ejecución.
 */
export interface EntornoDeEjecución {

	/**
	 * El nombre único del entorno.
	 */
	readonly nombre: string;

}